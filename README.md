The ConsolMatrix is library for using ANSI escapes to create "Matrix" on Consol

It's opensource, and doesn't use any outside libraries, only stdlib.

Licensed under Apache 2.0

litle trick:

normally conhost doesn't understand ANSI escape, but if you do conhost <program\_name> >nul - it will understand

